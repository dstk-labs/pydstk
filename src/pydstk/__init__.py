# Package versioning should conform to PEP 440
#
# Specifically, this project uses the syntax
# X.Y.Z-a
# where `X` is major version, `Y` is minor version,
# `Z` is patch version, and `a` optionally denotes
# pre-release version from <alpha|beta|rc>
__version__ = "0.1.0-alpha"